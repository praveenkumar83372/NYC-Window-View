name: NYC Window View 24-7

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout Code
        uses: actions/checkout@v3

      - name: 2. Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          pip install supabase requests

      - name: 3. Build Playlist
        env:
          SUPABASE_URL: "https://ofqbyabbjpincectjdya.supabase.co"
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python - <<EOF
          import os
          from supabase import create_client
          url = os.environ.get("SUPABASE_URL")
          key = os.environ.get("SUPABASE_KEY")
          supabase = create_client(url, key)
          response = supabase.table("playlist").select("url").execute()
          if not response.data:
              exit(1)
          with open("list.txt", "w") as f:
              for row in response.data:
                  # Use full path formatting for concat demuxer
                  f.write(f"file '{row['url']}'\n")
          EOF

      - name: 4. Start 1x Speed High-Bitrate Stream
        run: |
          # We use the 'concat' demuxer for efficiency but force 1x speed.
          # This keeps the image sharp and the 24/7 connection solid.
          while true; do
            echo "Starting NYC Stream: Enforcing 1x Real-time Speed..."
            
            ffmpeg -hide_banner -loglevel warning \
              -re -readrate 1 \
              -protocol_whitelist "file,http,https,tcp,tls" \
              -reconnect 1 -reconnect_at_eof 1 -reconnect_streamed 1 -reconnect_delay_max 5 \
              -f concat -safe 0 -i list.txt \
              -c:v libx264 -preset fast -tune zerolatency \
              -profile:v high -level 4.1 \
              -b:v 4500k -maxrate 4500k -bufsize 9000k \
              -pix_fmt yuv420p -g 60 \
              -vf "scale=720:1280:force_original_aspect_ratio=decrease,pad=720:1280:(ow-iw)/2:(oh-ih)/2,format=yuv420p" \
              -c:a aac -ar 44100 -b:a 128k -ac 2 \
              -af "aresample=async=1000:min_hard_comp=0.1:first_pts=0" \
              -f flv rtmp://a.rtmp.youtube.com/live2/${{ secrets.STREAM_KEY }}
            
            echo "Playlist finished. Looping back to top in 5 seconds..."
            sleep 5
          done